## koala cluster register

Register a new cluster

### Synopsis

Register a new cluster.

```
koala cluster register cluster-name [flags]
```

### Options

```
  -h, --help              help for register
  -l, --location string   Location (region or zone, e.g. europe-west1-b)
  -p, --project string    Project ID or AWS Account ID (e.g. my-project-1234)
```

### Options inherited from parent commands

```
  -o, --org string   Organization name
```

### SEE ALSO

* [koala cluster](/cli-reference/koala_cluster)	 - Cluster management commands


